{
  "name": "so-quy-gia-dinh",
  "version": "2.1.0",
  "description": "Sổ Quỹ Gia Đình — PWA quản lý thu chi gia đình (Firebase Realtime Database + IndexedDB offline), đóng gói thành app Android bằng Capacitor.",
  "private": true,
  "scripts": {
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:build:debug": "cap sync android && cd android-app && ./gradlew assembleDebug",
    "android:build:release": "cap sync android && cd android-app && ./gradlew assembleRelease"
  },
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
